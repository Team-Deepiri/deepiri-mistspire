@AGENTS.md

This file is a Claude Code import alias for [AGENTS.md](AGENTS.md) (the canonical agent rules). A Git symlink is unreliable across Windows vs Linux, so keep `@AGENTS.md` as the first line and edit rules in `AGENTS.md` only.
